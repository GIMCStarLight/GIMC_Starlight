# RESTful API Server Dependencies
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0
python-multipart>=0.0.6

# Core crawler dependencies (already in project)
# requests
# aiohttp
# asyncio-throttle
# psycopg2-binary (for PostgreSQL support)